<%= form_for(@doc_diver,:url => {:controller=>"doc_divers", :action => :save_doc},:html => { :multipart => true }) do |f| %>
  <% if @doc_diver.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doc_diver.errors.count, "error") %> prohibited this doc_diver from being saved:</h2>

      <ul>
      <% @doc_diver.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<label for="doc_Nom">Nom du document:</label>
  <%= f.text_field "Nom", :size => 20 %></p>
  <p><label for="doc_type_doc_id">type de document:</label>
<td>
<%=  f.select("type_doc_id", TypeDoc.all.collect {|p| [ p.abbrev,p.id ] })%><br/>
  </p>
  <p>
  <%= f.hidden_field "id_entite" %>
  <%= hidden_field_tag "retour",retour %>
  <label for="doc_doc">document:</label><%= file_field 'upload', 'datafile' %></p>
  <div class="actions">
  <%= f.submit 'Enregistrer' %>
  </div>
<% end %>
